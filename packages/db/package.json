{
  "name": "@caredesk/db",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json",
    "rls-test:ci": "node --import tsx src/rls-check-ci.ts",
    "migrate-idempotency:ci": "node --import tsx src/migrate-idempotency-ci.ts"
  },
  "dependencies": {
    "@caredesk/application": "workspace:*",
    "@caredesk/domain": "workspace:*",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/pg": "^8.11.10",
    "tsx": "^4.23.13",
    "typescript": "^5.7.3",
    "vitest": "^3.0.4"
  }
}
