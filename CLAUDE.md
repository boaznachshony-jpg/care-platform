# הוראות עבודה קבועות — CareDesk

> קובץ זה נקרא אוטומטית בתחילת כל שיחה שמתנהלת בספרייה הזו, על ידי כל סוכן.
> מטרתו לקבע כללים שסוכמו פעם אחת, כדי שלא יידרשו לחזור עליהם בכל פעם.

## 1. דיווח

- **כל הודעה למשתמש נפתחת בחותמת תאריך ושעה בפורמט `DD.MM.YYYY, HH:MM`.**
  השעה **נקראת מהמערכת** (`TZ=Asia/Jerusalem date '+%d.%m.%Y %H:%M'`) — לעולם לא
  מוערכת, לא מחושבת מהקשר ולא מומצאת. שעה שגויה בדיווח היא שקר קטן שמערער את
  האמון בכל השאר.
- שפת התקשורת עם המשתמש היא **עברית**.
- המשתמש **אינו איש טכני**. כל פעולה שהוא נדרש לבצע בעצמו תינתן כצעדים מדויקים
  להעתקה, עם ציון מפורש **היכן** מריצים אותה: `pnpm` ו-`cd` ← PowerShell;
  `select`/`insert` ← ה-SQL Editor של Supabase. אין לערבב.
- דיווח כולל את מה ש**לא** נעשה ואת מה שנכשל, לא רק את מה שהצליח.

## 2. נתוני לקוח — הכלל העליון

הבעלים קבע: **"נתוני לקוח הם שלו ולא ניתן לאבד אותם."** מכאן:

- אין פעולה שמזיזה נתוני לקוח אמיתיים מעל גיבוי שלא נוסה.
- כל מיגרציה היא additive עד שהוכח אחרת.
- תהליך עדכון גרסה לא פוגע בנתונים.
- כשל שמירה **חייב** להיראות על המסך. כתיבה שנכשלת בשקט היא הבאג החמור ביותר
  במוצר הזה, וכבר קרתה יותר מפעם אחת.

## 3. נוהל שחרור

`docs/governance/PRODUCTION-RELEASE-PROCEDURE.md` הוא הנוהל המחייב.
`docs/governance/PRODUCT-UX-IMPROVEMENT-BACKLOG.md` הוא המסמך החי שמתעדכן בכל סבב.

עיקרי הכללים שנוטים להישכח:

- **`COMPLETE` רק עם Evidence ואימות בייצור.** קוד שנכתב אינו משימה שהושלמה.
  הסדר: `IN_PROGRESS → IN_REVIEW → READY_FOR_PRODUCTION → DEPLOYED →
PRODUCTION_VERIFIED → COMPLETE`.
- **אין Big Bang.** Release אחד = מספר PRs קטנים לפי bounded concern.
- פער חדש שמתגלה מקבל **שורה חדשה ב-Backlog**, ולא הערה מוסתרת בתוך פריט אחר.
- פריט שכבר מכוסה ב-Remediation קיים מסומן `ALREADY_PLANNED` עם קישור, במקום
  להיפתח מחדש.

## 4. קצב עבודה

- **תמיד להתקדם למשימה הבאה — אין צורך להמתין לאישור בין שלבים.**
- לעבוד במקביל כשניתן (סוכנים מרובים), ולמזער המתנות של המשתמש.
- לחזור למשתמש רק כשנדרשת החלטת מוצר, או פעולה שרק הוא יכול לבצע (דחיפה
  ל-GitHub, לחיצה בדפדפן, הגדרה ב-Vercel/Supabase).

## 5. מגבלות סביבה ידועות

- `git push` חסום מהסביבה של הסוכן (proxy) — המשתמש דוחף מ-PowerShell.
- `vitest` ו-Playwright אינם ניתנים להרצה מהסביבה (`node_modules` בנוי ל-Windows,
  אין דפדפנים) — הרצה אמיתית מתבצעת ב-CI או אצל המשתמש. **אין לטעון שבדיקות
  עברו אם לא נצפתה ריצה.**
- מה שכן ניתן להריץ ויש להריץ לפני כל commit: `tsc --noEmit`, `eslint`,
  `prettier --check`, וששת סקריפטי ה-guard שב-`scripts/`.
- נעילות git תקועות: ראה `docs/governance/GIT-STUCK-LOCKS.md`.

## 6. תחומי המוצר

מוצר עברי/RTL למשפחות ישראליות המעסיקות מטפל/ת זר/ה בהעסקה ישירה.

- כסף: **אגורות שלמות בלבד**, ב-`packages/domain/src/payroll.ts`. אין floats.
- מקור אמת: הטבלאות הקנוניות ב-PostgreSQL (ADR-006). `caredesk.mvp.*` בדפדפן הוא
  מנגנון מעבר בלבד ואינו מקור אמת.
- Forecast אינו Actual. Score אינו אישור לעמידה בדין. חישוב אינו אמת משפטית.
- תהליך סיום העסקה: פירוט התאגיד הוא הקובע; הפער מול החישוב הוא **חוב לעובד**.
  ראה `docs/governance/TERMINATION-SETTLEMENT.md`.
